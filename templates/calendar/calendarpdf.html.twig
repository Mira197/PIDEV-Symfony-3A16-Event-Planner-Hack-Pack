<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Calendar PDF</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            padding: 30px;
            color: #333;
        }

        h1, h2 {
            color: #6b336e;
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            box-shadow: 0 0 12px rgba(0,0,0,0.1);
        }

        th, td {
            padding: 10px;
            border: 1px solid #ddd;
            text-align: center;
        }

        th {
            background-color: #99707E;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f9f6f9;
        }

        .status-booked {
            background-color: #e74c3c;
            color: white;
            font-weight: bold;
        }

        .status-not-booked {
            background-color: #2ecc71;
            color: white;
            font-weight: bold;
        }

        .legend {
            margin-top: 20px;
            text-align: center;
        }

        .legend span {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 4px;
            margin: 0 10px;
            color: white;
            font-size: 14px;
        }

        footer {
            margin-top: 40px;
            font-size: 12px;
            text-align: center;
            color: #888;
        }
    </style>
</head>
<body>

    <img src="{{ absolute_url(asset('images/logo2.png')) }}" style="width:100px; display:block; margin:auto;">
    <h1>My Event Planning Summary</h1>

    <table>
        <thead>
            <tr>
                <th>Title</th>
                <th>Start</th>
                <th>End</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            {% for event in events %}
                <tr>
                    <td>{{ event.name }}</td>
                    <td>{{ event.startDate|date('Y-m-d H:i') }}</td>
                    <td>{{ event.endDate|date('Y-m-d H:i') }}</td>
                    <td class="{{ event.booked ? 'status-booked' : 'status-not-booked' }}">
                        {{ event.booked ? 'Booked' : 'Not Booked' }}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    {#some stats#}
    <div style="margin-top: 30px; text-align: center; font-size: 16px;">
        <p><strong>Total Events:</strong> {{ total }}</p>
        <p><strong>Booked:</strong> {{ booked }} ({{ (booked / total * 100)|number_format(0) }}%)</p>
        <p><strong>Not Booked:</strong> {{ total - booked }} ({{ (100 - (booked / total * 100))|number_format(0) }}%)</p>
    </div>


    <footer>
        Generated on {{ "now"|date("Y-m-d H:i") }} | 3ala-Kifi Event Planner ©
    </footer>

</body>
</html>
