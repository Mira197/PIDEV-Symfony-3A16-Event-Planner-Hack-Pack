<form method="post" action="{{ path('location_admin_delete', {'id': location.idLocation}) }}" onsubmit="return confirm('Are you sure you want to delete this location?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ location.idLocation) }}">
    <button class="btn btn-danger">Delete</button>
</form>
